# Optional development default. Native users can select their server in Settings instead.
# This value is public application configuration, never a bearer token or other secret.
EXPO_PUBLIC_MONGARS_API_URL=https://mongars.example.com
